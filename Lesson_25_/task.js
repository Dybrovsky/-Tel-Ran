function maskify(cc) {

    for(i = 0; i > cc.length; i++)
        let res = "";
        let el = cc[i] = "#";
        res += res + el;

        return res;
}

let a = "dhdhdhdhdhdhdh";
console.log(maskify(a));
