let str = "123456789";

function maskify(number) {
    // for (let i = number.length-1; i >= 0; i--) {
    //     console.log(number[i]);
    // let res = ;
    // ]
    /}

    return number.split('').map(letter, number.length-1)
    return number.split('').map(letter, number.length-1)

}

maskify('123456');